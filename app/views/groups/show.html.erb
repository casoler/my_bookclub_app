<h3><%= @group.name %></h3>

<p>
  Leader: 
    <% if @member.group_leader? %>
      <%= @member.user.first_name %> <%= @member.user.last_name %>
    <% else %>
      No Leader
    <% end %>
</p>

<p>
  <div>
    Members:
    <% @group.members.each do |member| %>
      <%= member.user.first_name %>
      <%= member.user.last_name %>
    <% end %>
  </div>
</p>

<div>
  <% if @member.group_leader? %>
    <%= link_to "Make a suggestion list for next month's book", books_path(member_id: @member.id) %>
    <%= link_to "Select next month's book", api_v1_book_votes_path(group_id: @group.id) %>
  <% end %>
</div>



<div>
  This month's book: 
</div> 

<div>
  Past Books:
  <% @group.past_books.each do |past_book| %>
    <%= past_book.book.title %>
  <% end %>
</div>